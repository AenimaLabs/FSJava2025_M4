package utilidadesMatematicas;

import org.junit.platform.suite.api.SelectClasses;
import org.junit.platform.suite.api.Suite;

@Suite
@SelectClasses({
        UtilidadesMatematicasTest.class
        //Se pueden agregar mas tests aqui
})

public class TestSuite {
    //Esta clase permanece vacía; se usa solo como contenedor de la suite
}
